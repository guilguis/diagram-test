import { IConfigState } from "./types";

export const defaultConfigs:IConfigState = {
    canvasName: '',
    canvasWidth: 1920,
    canvasHeight: 1080,
    gridActive: true,
    gridSnap: false,
    gridSize: 15
}